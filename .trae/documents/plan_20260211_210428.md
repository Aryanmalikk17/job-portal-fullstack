The `docker-compose.prod.yml` uses environment variables (`${MYSQL_PASSWORD}`), so we need to see the actual values from the `.env` file to know what password the backend is trying to use.

### Plan: Get Password and Reset DB User

**1. Read the .env file (Run on Server)**
We need to find the value of `MYSQL_PASSWORD` and `MYSQL_ROOT_PASSWORD` inside `/opt/jobportal/.env`.

cat /opt/jobportal/.env

**2. Reset MySQL User Password (Run on Server)**
Once you provide the output of the `.env` file, I will construct the exact MySQL command to reset the `jobportal` user's password to match `MYSQL_PASSWORD`.

The command will look like this (don't run it yet, wait for the actual password):

```bash
docker exec -it jobportal-mysql-prod mysql -u root -p
# Enter root password from .env
ALTER USER 'jobportal'@'%' IDENTIFIED BY 'ACTUAL_PASSWORD_FROM_ENV';
FLUSH PRIVILEGES;
```

**Action for You:**
Please run `cat /opt/jobportal/.env` on the server and paste the output.
