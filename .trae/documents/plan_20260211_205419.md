The user's logs confirm the following:

1.  **Backend Crash:** The backend is restarting (`Starting JobPortalApplication...` appears multiple times).
2.  **Database Connection Failure:**
    ```
    Caused by: java.sql.SQLException: Access denied for user 'jobportal'@'172.18.0.4' (using password: YES)
    ```
3.  **Nginx 502:** Nginx returns 502 because the backend container is crashing/restarting due to the DB error.

### The Fix: Reset Database Password

The backend expects a password that differs from what is set in MySQL. We need to align them.

**Plan:**
1.  **Identify Expected Password:** Ask the user to grep the password from `docker-compose.prod.yml` on the server.
2.  **Reset MySQL Password:** Provide the command to reset the MySQL user's password to match.
3.  **Restart Backend:** Restart the backend container.

I will ask the user to find the password first.