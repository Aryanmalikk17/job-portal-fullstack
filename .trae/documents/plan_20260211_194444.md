I will proceed with the deployment and verification as requested.

**1. Prepare Local Codebase**
*   **Git Commit**: Stage and commit all changes, including the fixes in `backend/` and the updated `verify_api.sh`.
*   **Git Push**: Push the changes to `origin main`.

**2. Deploy to Production**
*   **Method A (SSH & Git Pull)**: I will attempt to execute the deployment commands remotely via SSH:
    *   `ssh root@64.227.189.10 "cd /opt/jobportal && git pull origin main && docker-compose -f docker-compose.prod.yml up -d --build app"`
*   **Method B (Fallback)**: If Method A fails (e.g., due to SSH keys/permissions), I will attempt to SCP the backend source and rebuild:
    *   `scp -r backend/src root@64.227.189.10:/opt/jobportal/backend/`
    *   `ssh root@64.227.189.10 "cd /opt/jobportal && docker-compose -f docker-compose.prod.yml up -d --build app"`

**3. Post-Deployment Verification**
*   **Health Check**: Run `curl https://zplusejobs.com/api/actuator/health` to confirm the backend is up.
*   **Critical Fix Verification**:
    *   **Profile Update**: Simulate a profile update to confirm the "Duplicate Entry" fix.
    *   **My Applications**: Query the "My Applications" endpoint to confirm the 500 error fix.
*   **Logs**: Check remote logs via SSH (`docker logs jobportal-app --tail 100`) to ensure no runtime errors.

I will start by committing the local changes.