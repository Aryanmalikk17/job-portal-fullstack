Thank you for providing the key paths. I will attempt to use these keys to authenticate with the server. Note that `cert.pem` is usually a public certificate and `key.pem` is the private key. I will try using `ssl/key.pem`.

**1. Attempt Deployment with Provided Key**
*   **Action**: Try to SSH and deploy using the provided private key.
    ```bash
    ssh -i ssl/key.pem root@64.227.189.10 "cd /opt/jobportal && git pull origin main && docker-compose -f docker-compose.prod.yml up -d --build app"
    ```

**2. Contingency: Generate New SSH Key (If Access Fails)**
*   If the provided key does not work (Permission denied), it means the public key corresponding to `key.pem` is not in `~/.ssh/authorized_keys` on the server.
*   In this case, I cannot "generate a new key" that will magically grant access. **You would need to add the new public key to the server manually** (e.g., via DigitalOcean Console).
*   **Plan B**: I will guide you on how to generate a new key pair and where to add it on the server console.

**3. Verify Production**
*   Once access is gained and deployment runs, I will verify the production health check.

I will proceed with **Step 1** immediately.