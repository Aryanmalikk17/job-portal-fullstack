The error `no such service: app` indicates that the service name in `docker-compose.prod.yml` is not `app`. It is likely named `backend` based on previous context.

### Plan: Deploy using Correct Service Name

**1. Deploy with Correct Service Name (Run on Server)**
Try building the `backend` service instead:

```bash
docker-compose -f docker-compose.prod.yml up -d --build backend
```

*If that also fails, list the services to confirm the name:*
```bash
grep "service" -A 1 docker-compose.prod.yml
# or just cat the file
cat docker-compose.prod.yml
```

**2. Verify (Run on Server)**
```bash
curl http://localhost:8080/actuator/health
```