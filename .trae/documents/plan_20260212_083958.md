The error `Database is uninitialized` means the volume name I provided (`jobportal-mysql-data`) is likely incorrect, so MySQL thinks it's starting from scratch and requires a password.

We need to use the exact volume name created by Docker Compose.

### Plan: Identify Correct Volume and Reset

**1. Find the Correct Volume Name (Run on Server)**
```bash
docker volume ls | grep mysql
```
*(Look for something like `jobportal_mysql_data_prod` or `jobportal-mysql-data-prod`)*

**2. Start Recovery with Correct Volume (Run on Server)**
Replace `CORRECT_VOLUME_NAME` with the name you found in Step 1.

```bash
docker rm mysql-recovery || true
docker run --name mysql-recovery -v CORRECT_VOLUME_NAME:/var/lib/mysql -e MYSQL_ALLOW_EMPTY_PASSWORD=yes -d mysql:8.0 --skip-grant-tables
```

**3. Reset Passwords (Run on Server)**
```bash
docker exec -it mysql-recovery mysql
```
*(Paste SQL)*
```sql
FLUSH PRIVILEGES;
ALTER USER 'root'@'localhost' IDENTIFIED BY 'Zpluse@2024!';
ALTER USER 'root'@'%' IDENTIFIED BY 'Zpluse@2024!';
CREATE USER IF NOT EXISTS 'jobportal'@'%' IDENTIFIED BY 'Zpluse@2024!';
ALTER USER 'jobportal'@'%' IDENTIFIED BY 'Zpluse@2024!';
GRANT ALL PRIVILEGES ON jobportal.* TO 'jobportal'@'%';
FLUSH PRIVILEGES;
EXIT;
```

**4. Restore (Run on Server)**
```bash
docker stop mysql-recovery
docker rm mysql-recovery
docker start jobportal-mysql-prod
docker-compose -f docker-compose.prod.yml restart backend
```

**Immediate Action for You:**
Please run `docker volume ls | grep mysql` on the server and tell me the volume name.