### **Diagnosis: Two Critical Issues Found**

1.  **Backend "Hanging" caused by Mail Server Timeout**:
    *   The logs explicitly show: `Health contributor ... MailHealthIndicator (mail) took 133571ms to respond` and `java.net.ConnectException: Operation timed out` connecting to `SMTP`.
    *   This is why `curl localhost:8080` was hanging! The backend is trying to check the Email Server status as part of its health check, and it's timing out because the SMTP connection is blocked or misconfigured.
    *   **Impact**: The backend reports as "Unhealthy" or "DOWN" because it can't reach the mail server.

2.  **"Invalid user type" caused by Empty Database**:
    *   The command `SELECT * FROM users_type;` returned **nothing** (empty output).
    *   This confirms the database is empty. The initialization script didn't run or populate this table.
    *   **Impact**: Registration fails because the backend expects user types (ID 1=Recruiter, 2=Job Seeker) to exist in the DB.

### **The Fix Plan**

**Step 1: Fix Database Data (Immediate Priority)**
We will manually insert the missing user types so you can register.

**Step 2: Fix Mail Timeout (Secondary)**
We will temporarily disable the Mail Health Check so the backend doesn't hang and report "DOWN".

### **Execution**

**1. Populate Database (Run on Server)**
```bash
docker exec -i jobportal-mysql-prod mysql -u jobportal -pZpluseAppPass2025! -D jobportal_db <<EOF
INSERT INTO users_type (user_type_id, user_type_name) VALUES (1, 'Recruiter');
INSERT INTO users_type (user_type_id, user_type_name) VALUES (2, 'Job Seeker');
EOF
```

**2. Verify Data**
```bash
docker exec -it jobportal-mysql-prod mysql -u jobportal -pZpluseAppPass2025! -D jobportal_db -e "SELECT * FROM users_type;"
```
*(You should now see the rows)*

**3. Disable Mail Health Check (To fix hanging)**
I will update `application-prod.properties` to disable the mail health check so your backend becomes "Healthy" even if email is broken for now.

Let's start with **Step 1 & 2** (Database Fix) so you can test registration!
