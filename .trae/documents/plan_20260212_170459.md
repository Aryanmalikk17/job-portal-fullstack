Great, the commands ran successfully! You have now given ownership of the `/opt/jobportal` folder to the `ubuntu` user.

### **Next Steps**

1.  **Go to GitHub Actions:**
    *   Navigate to your repository on GitHub.
    *   Click the **Actions** tab.
    *   Select the failed workflow run.
    *   Click **Re-run jobs** (top right) > **Re-run all jobs**.

2.  **Monitor the Run:**
    *   If it turns **Green (Success)**: Your application is deployed! Check your website.
    *   If it fails again with **"Permission denied"**:
        *   This means your `SERVER_USER` secret is likely **`deploy`** (the other user we saw in `/home`) instead of `ubuntu`.
        *   **Fix:** Run this command on your server: `chown -R deploy:deploy /opt/jobportal` and try re-running the action again.

Let me know the result!
