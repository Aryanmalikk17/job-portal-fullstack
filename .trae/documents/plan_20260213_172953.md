Okay, I see what's happening now. The logs show:
`Access denied for user 'jobportal'@'%' to database 'jobportal_db'`

Wait, notice the database name in the error: **`jobportal_db`**.

But in your `docker-compose.prod.yml`, you are setting:
```yaml
MYSQL_DATABASE: jobportal
```

And in your GitHub Actions `deploy.yml` (and thus `.env`), we set `DB_NAME` to match `MYSQL_DATABASE`.

However, the backend is trying to connect to **`jobportal_db`**.

This means your **Backend Code** (Spring Boot) is likely hardcoded or defaulting to `jobportal_db` somewhere, OR the `DB_NAME` environment variable isn't being picked up correctly by the application properties.

Let's look at `application-prod.properties` again.
It has: `spring.datasource.url=${DB_URL:jdbc:mysql://mysql:3306/jobportal}`
And `docker-compose` sets `DB_URL` to:
`jdbc:mysql://mysql:3306/${DB_NAME}?useSSL=false...`

If `${DB_NAME}` in `.env` is `jobportal`, then the URL should be `.../jobportal`.

**Why is it trying to access `jobportal_db`?**

Let's check if the user has a `DB_NAME` variable set in their secrets that is `jobportal_db`?
In the `deploy.yml`, we used:
```yaml
DB_NAME=${{ secrets.MYSQL_DATABASE }}
```
If the user's secret `MYSQL_DATABASE` is `jobportal_db`, then:
1.  The MySQL container creates a database named `jobportal_db`.
2.  The backend tries to connect to `jobportal_db`.

**BUT**, the error is `Access denied ... to database 'jobportal_db'`.
This implies the **user 'jobportal'** does not have permission to access **'jobportal_db'**.

If `MYSQL_DATABASE` env var is set on the MySQL container, it grants the `MYSQL_USER` access to **that specific database only**.

If the backend is trying to access `jobportal_db`, but the MySQL container created `jobportal` (or vice versa), that's the mismatch.

**Let's Verify the Database Name Mismatch:**
1.  User's `docker-compose` has `MYSQL_DATABASE: jobportal` (hardcoded line 10).
2.  Backend `DB_URL` uses `${DB_NAME}`.
3.  Deploy script sets `DB_NAME=${{ secrets.MYSQL_DATABASE }}`.

**Conflict:**
*   **MySQL Container** is created with `MYSQL_DATABASE: jobportal` (hardcoded in `docker-compose.prod.yml`).
*   **Backend** is configured with `DB_NAME` from secrets. If your secret `MYSQL_DATABASE` is `jobportal_db`, then the backend tries to connect to `jobportal_db`.
*   **Result:** MySQL has a DB named `jobportal`. Backend tries to reach `jobportal_db`. User `jobportal` only has access to `jobportal`. Hence "Access denied to database jobportal_db".

### **The Fix**
We must make sure the database name in `docker-compose.prod.yml` matches the one in your secrets/backend configuration.

I will modify `docker-compose.prod.yml` to use the environment variable `${DB_NAME}` (or `${MYSQL_DATABASE}`) instead of hardcoding `jobportal`.

**Plan:**
1.  **Edit `docker-compose.prod.yml`**: Change `MYSQL_DATABASE: jobportal` to `MYSQL_DATABASE: ${DB_NAME}`.
2.  **Commit and Push**.
3.  **Deploy**: This will ensure both the database created and the backend connection string use the **exact same name** (whatever is in your `.env` file).

**Wait**, `docker-compose.prod.yml` uses `MYSQL_DATABASE: jobportal` on line 10.
But the `.env` file generated by deploy script has `DB_NAME`.
We should use `${DB_NAME}` in the mysql service too.

**Let's do this:**
1.  Modify `docker-compose.prod.yml` to use `MYSQL_DATABASE: ${DB_NAME}`.
2.  This ensures consistency.

**Also**, we need to verify what is in the `.env` file on the server for `DB_NAME`.
If the user can run `grep DB_NAME .env` on the server, we will know for sure.

But fixing the code is the right path.

### **Revised Plan**
1.  **Update `docker-compose.prod.yml`**:
    *   Change `MYSQL_DATABASE: jobportal` -> `MYSQL_DATABASE: ${DB_NAME}`.
2.  **Commit & Push**.
3.  **Redeploy (Clean)**: Since we are changing the DB name potentially, a volume wipe is safest again.

I'll start by fixing the compose file.
